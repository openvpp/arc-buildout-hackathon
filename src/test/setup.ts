import '@testing-library/jest-dom/vitest';

// React Testing Library registers automatic DOM cleanup after each test via the
// global `afterEach` (Vitest `globals: true`), so no manual cleanup is needed.
